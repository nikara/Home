/*
Descreva o conceito de Fila em ciência da computação, incluindo suas principais características e tipos. Em sua resposta, explique o princípio FIFO (First In, First Out) e forneça exemplos de situações práticas em que filas são utilizadas.
Crie um programa que utilize um vetor (array) e implemente manualmente os métodos descritos, sem usar bibliotecas prontas da linguagem, garantindo que todas as operações sejam realizadas corretamente sem o uso de bibliotecas prontas.


Requisitos

Não utilizar bibliotecas prontas da linguagem.
Implementar manualmente as funcionalidades do ArrayList.


Funcionalidades

Criar um menu com as seguintes opções para o usuário:

Inserir elementos em um vetor
Na próxima posição livre do vetor 
Na posição informada 
Pesquisar um elemento no vetor 
Pelo elemento informado 
Pela posição do vetor 
Excluir um elemento do vetor 
Excluir o elemento informado
Exclui o elemento que está na posição informada
Imprimir o vetor


Explicação do que deve ser feito em cada opção do menu: 

Inserir elementos em um vetor
Na próxima posição livre do vetor: o usuário deve poder adicionar um novo elemento na próxima posição livre do vetor. O programa deve verificar se o vetor tem espaço suficiente para inserir esse novo elemento, se tiver, ele adiciona, caso contrário, ele emite uma mensagem ao usuário informando que o vetor está cheio.
Na posição informada: o usuário deve poder inserir um novo elemento em uma posição específica do vetor informada pelo próprio usuário. O programa deve deslocar os elementos existentes para abrir espaço para o novo elemento.
Pesquisar um elemento no vetor
Pelo elemento informado: o usuário deve poder pesquisar um elemento específico no vetor. O usuário deve informar o elemento que deseja pesquisar. O programa deve percorrer o vetor e retornar à posição do elemento, se encontrado, ou uma mensagem indicando que o elemento não foi encontrado.
Pela posição do vetor: o usuário deve poder acessar um elemento em uma posição específica do vetor. O programa deve retornar o valor do elemento na posição informada pelo usuário.
Excluir um elemento do vetor
Excluir o elemento informado: o usuário deve poder remover um elemento específico do vetor. O usuário informa o elemento que deseja excluir. O programa deve encontrar o elemento e removê-lo, deslocando os elementos subsequentes para preencher o espaço deixado pelo elemento removido.
Excluir o elemento que está na posição informada: o usuário deve poder remover um elemento em uma posição específica do vetor. O usuário deverá informar a posição que deseja remover o elemento. O programa deve deslocar os elementos subsequentes para preencher o espaço deixado pelo elemento removido.
Imprimir o vetor: o usuário deve poder visualizar todos os elementos do vetor. O programa deve percorrer o vetor e imprimir cada elemento em ordem.
Encerrar o programa: o usuário deve poder encerrar a execução do programa. 

*/

#include <stdlib.h>
#include <stdio.h>

#define SIZE 1000;

int main(){
	
	// Loop geral
	
	while(){
		
	}
	
	
	
	return 0;
}